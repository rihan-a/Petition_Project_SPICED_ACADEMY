<div class="page-wrapper">
    <div class="petition-home">
        {{! <img src="./images/ubahn.jpeg" alt="" /> }}
        <div class="petition-title">
            <h1>Bring the 9 euro</h1>
            <h1>ticket back!</h1>
            <h2>But we won't get there without your support. Join our campaign
                today!</h2>
        </div>
    </div>

    <form type="submit" method="post" id="registration-form">
        <input type="text" placeholder=" Your first name" name="firstname" />
        <input type="text" placeholder=" Your last name" name="lastname" />
        <input type="email" placeholder=" Your email" name="email" />
        <input type="password" placeholder=" Your password" name="password" />
        <input type="submit" value="Create Account" />
    </form>
    <div class="login-wrapper">
        <p class="already-member">Already a member?
            <a href="/login">Log-in</a></p>
        <p class="form-error">{{error}}</p>
    </div>
</div>